<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login | Acceleration Tutorials</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css" />
  </head>

  <body class="bg-dark text-light d-flex align-items-center justify-content-center" style="min-height:100vh;">
    <div class="card p-4 shadow-lg" style="max-width:400px; width:100%; background:rgba(255,255,255,0.05); border-radius:14px;">
      <div class="text-center mb-3">
        <i class="bi bi-lightning-charge-fill text-warning fs-2"></i>
        <h4 class="fw-bold text-warning">Welcome Back</h4>
        <p class="text-muted small">Sign in to your account</p>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger py-2 small"><%= error %></div>
      <% } %>

      <form action="/login" method="POST">
        <div class="mb-3">
          <label class="form-label small">Email Address</label>
          <input type="email" name="email" class="form-control bg-transparent text-light border-secondary" placeholder="your@email.com" required>
        </div>
        <div class="mb-3">
          <label class="form-label small">Password</label>
          <input type="password" name="password" class="form-control bg-transparent text-light border-secondary" placeholder="••••••••" required>
        </div>
        <button type="submit" class="btn btn-warning w-100 fw-semibold">Login</button>
      </form>
      <div class="mt-3 text-center">
        <a href="https://drive.google.com/drive/folders/1g1Bvpb3wEfGbGN1G1WJyk8kFMFM92zzf?usp=sharing" target="_blank" rel="noopener noreferrer" class="btn btn-outline-light btn-sm">
          <i class="bi bi-google me-1"></i> Course Resources (Google Drive)
        </a>
      </div>

      <p class="text-center mt-3 small text-muted">
        New here? <a href="/register" class="text-warning text-decoration-none fw-semibold">Create Account</a>
      </p>
    </div>
  </body>
</html>
